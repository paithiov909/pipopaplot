% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as-notes.R
\name{as_notes}
\alias{as_notes}
\alias{as_notes.default}
\alias{as_notes.ggplot}
\title{Convert data to a standardized notes tibble}
\usage{
as_notes(d, ...)

\method{as_notes}{default}(d, ...)

\method{as_notes}{ggplot}(d, ..., .id = 1)
}
\arguments{
\item{d}{A data frame or a \code{ggplot2} object.
When a \code{ggplot2} object is given, the layer data is extracted using
\code{\link[ggplot2:ggplot_build]{ggplot2::get_layer_data()}} with the layer index specified by \code{.id}.}

\item{...}{Optional name mappings.
Provide named arguments to override the default column names
(e.g., \code{as_notes(d, duration = "fill")} will use \code{fill} as the duration column).}

\item{.id}{Layer index to extract from a ggplot object. Defaults to \code{1}.}
}
\value{
A tibble with columns:
\itemize{
\item \code{x}, \code{y} – coordinate-like values to be mapped to time and pitch
\item \code{channel} – grouping for MIDI channels
\item \code{group} – sub-group within each channel
\item \code{duration}, \code{velocity} – numeric modifiers for note length and intensity
}
}
\description{
\code{as_notes()} converts an arbitrary data frame or a \code{ggplot} object into
a tibble containing the required columns for MIDI sonification:
\code{x}, \code{y}, \code{channel}, \code{group}, \code{duration}, and \code{velocity}.
}
\details{
The returned tibble serves as the standard 'notes' format accepted by \code{\link[=sonify]{sonify()}}.
Numeric or logical columns are converted to numeric and missing values are replaced with \code{1}.
}
